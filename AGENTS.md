# Agent Notes

Before making code changes, read:

- `docs/guidelines.md` for repository coding/test ownership rules.
- `docs/model.md` for current `musak_model` pipeline, token semantics, and generation constraints.
